#ifndef RANDOMIZE_H
#define RANDOMIZE_H

#include "lcg.h"

// template<typename ArrayType>
// void randomize_array(ArrayType array) {
//     int max_i = array.size();

// }



// RandomizeArray(String: array[])
// Integer: max_i = <верхняя граница массива>
// For i = 0 To max_i - 1
// // Выбираем элемент для i-й позиции в массиве.
// Integer: j = <псевдослучайный номер между i и max_i включительно>
// <Меняем местами значения величин array[i] и array[j].>
// Next i
// End RandomizeArray

#endif // RANDOMIZE_H